% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_conservation_authority}
\alias{retrieve_conservation_authority}
\title{Retrieve Conservation Authority administrative areas}
\usage{
retrieve_conservation_authority(
  simplify = TRUE,
  refresh = FALSE,
  max_age = NULL
)
}
\arguments{
\item{simplify}{Logical. If \code{TRUE} (the default), requests generalized
geometry from the service. Conservation Authority boundaries are
province-scale watersheds; simplification keeps the payload small without
losing analytic utility for most crosswalk use-cases.}

\item{refresh}{Logical. If \code{TRUE}, bypasses any cached copy and re-fetches
from the live API. Defaults to \code{FALSE}.}

\item{max_age}{Numeric or \code{NULL}. Maximum acceptable cache age in days;
older entries are re-fetched. Defaults to \code{NULL}.}
}
\value{
An \code{sf} object of Conservation Authority boundary polygons, with
\code{source_url}, \code{source_name}, and \code{retrieved_at} attributes attached for
provenance.
}
\description{
Retrieves Conservation Authority administrative area boundaries from the LIO
Open Data REST service (\code{LIO_Open03/11}).
}
\examples{
\dontrun{
# Retrieves from the Ontario LIO REST service and caches the result.
ca_areas <- retrieve_conservation_authority()
}

}
