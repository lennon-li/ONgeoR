% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{map_nearest}
\alias{map_nearest}
\title{Map nearest targets and their connections to source points}
\usage{
map_nearest(source, target, k = 1, max_dist_km = NULL)
}
\arguments{
\item{source}{An \code{sf} object of points, or a \code{data.frame} with \code{lon} and
\code{lat} columns (assumed CRS 4326 / WGS 84).}

\item{target}{An \code{sf} object of candidate point or polygon geometries.}

\item{k}{Integer. Number of nearest targets to map per source. Defaults to
\code{1}; \code{Inf} may be used with \code{max_dist_km} for a radius search.}

\item{max_dist_km}{Numeric or \code{NULL}. If set, omit targets farther than this
distance in kilometres.}
}
\value{
A \code{leaflet} htmlwidget.
}
\description{
Combines \code{\link[=nearest]{nearest()}} with \code{\link[=map_layers]{map_layers()}} to show source points, the targets
matched to them, and a connector line for every match. Only matched targets
are drawn. If no target is within \code{max_dist_km}, the returned map contains
the source points without target or connector layers.
}
\examples{
points <- data.frame(lon = -79.3832, lat = 43.6532)
map_nearest(points, retrieve_hive()[1:50, ], k = 3)

}
