% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postal.R
\name{resolve_postal_points}
\alias{resolve_postal_points}
\title{Resolve Ontario postal codes to point coordinates}
\usage{
resolve_postal_points(x, as_sf = FALSE)
}
\arguments{
\item{x}{Character vector of Ontario postal codes.}

\item{as_sf}{Logical scalar. If \code{FALSE} (default), return a tibble. If
\code{TRUE}, return an \code{sf} POINT layer in EPSG:4326.}
}
\value{
With \code{as_sf = FALSE}, a \code{\link[tibble:tibble]{tibble::tibble()}} with one row per input
element, in input order: postal code, coordinates, point provenance,
source URL, and retrieval time. With \code{as_sf = TRUE}, an \code{sf} POINT layer
in EPSG:4326 containing only the rows with coordinates. An unmatched
postal code, or a matched code whose \code{point_source} is \code{"none"}, yields
one row with \code{NA} coordinates; a single combined warning lists all
unmatched postal codes.
}
\description{
Downloads and verifies the immutable OPCC M1 postal-code centroid release,
then caches the parsed table locally. Postal codes are matched after
uppercasing and normalizing whitespace.
}
\section{Coverage}{

The release covers 299,796 Ontario postal codes: 282,409 matched by
\code{nar_centroid} (address-derived coordinates), 17,373 by \code{geonames}
(place-level coordinates, coarser), and 14 reported as \code{none}, which have
no coordinates and are returned as \code{NA}.
}

\examples{
\dontrun{
resolve_postal_points(c("K1A0B1", "M5V 3A8"))
resolve_postal_points("M5V 3A8", as_sf = TRUE)
}

}
