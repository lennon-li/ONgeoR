% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersection.R
\name{build_nearest_pairs}
\alias{build_nearest_pairs}
\title{Build a point-to-point nearest-match table}
\usage{
build_nearest_pairs(source, target)
}
\arguments{
\item{source}{An \code{sf} point layer (the candidate matches).}

\item{target}{An \code{sf} point layer (the units to be matched).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per target feature. Fixed columns
match \code{\link[=build_intersection]{build_intersection()}}; \code{relation} is \code{"nearest"},
\code{match_distance_km} is populated, and \code{overlap_area_m2},
\code{share_of_target}, \code{share_of_source} are \code{NA}.
}
\description{
Matches each target point to its single nearest source point. Returns the
same column schema as \code{\link[=build_intersection]{build_intersection()}} so the two are row-bindable.
}
\examples{
if (interactive()) {
  stations <- retrieve_orwn_station()
  airports <- retrieve_airport()
  pairs <- build_nearest_pairs(stations, airports)
}

}
