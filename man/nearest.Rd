% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{nearest}
\alias{nearest}
\title{Find the nearest targets to each source geometry}
\usage{
nearest(source, target, k = 1, max_dist_km = NULL)
}
\arguments{
\item{source}{An \code{sf} object of points, or a \code{data.frame} with \code{lon}/\code{lat}
columns (assumed CRS 4326 / WGS 84).}

\item{target}{An \code{sf} object of candidate geometries.}

\item{k}{Integer. Number of nearest targets to return per source. Must be a
single positive whole number, or \code{Inf} to return every target. Defaults
to \code{1}. If a source has fewer than \code{k} targets available, all are returned.}

\item{max_dist_km}{Numeric or \code{NULL}. If set, drop targets farther than this
distance (km). Defaults to \code{NULL} (no cap). A source with no target in
range contributes zero rows.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the source columns, \code{rank} (1 = nearest),
the matched target columns, \code{distance_km}, and \code{source_url} / \code{target_url}
/ \code{retrieved_at} provenance columns. Uses a full source-by-target distance
matrix (not spatial-indexed); requests over 10 million distances abort as
unsuitable at this scale. See the nearest-neighbour performance item in
\code{ROADMAP.md}.
}
\description{
For each source geometry, returns the \code{k} nearest targets in ascending
distance, optionally capped at \code{max_dist_km}. Use \code{k = Inf} with
\code{max_dist_km} for a pure radius search.
}
\examples{
stations <- retrieve_monitoring_stations_simple()[1:5, ]
nearest(stations, retrieve_hive()[1:50, ], k = 3)

}
