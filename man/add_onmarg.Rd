% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onmarg.R
\name{add_onmarg}
\alias{add_onmarg}
\title{Attach ON-Marg columns to an administrative boundary layer}
\usage{
add_onmarg(
  x,
  geography = NULL,
  scores = TRUE,
  quintiles = TRUE,
  population = FALSE,
  dimensions = names(onmarg_measure_stems()),
  refresh = FALSE
)
}
\arguments{
\item{x}{A data frame or \code{sf} object carrying the geography's key column -
for example the output of \code{\link[=retrieve_census]{retrieve_census()}} or \code{\link[=retrieve_phu]{retrieve_phu()}}.}

\item{geography}{Character scalar naming the ON-Marg geography, as in
\code{\link[=retrieve_onmarg]{retrieve_onmarg()}}. \code{NULL} (the default) detects it from the key columns
present in \code{x}, and errors if that is ambiguous or absent.}

\item{scores}{Logical. Attach the four factor-score columns. Default \code{TRUE}.}

\item{quintiles}{Logical. Attach the four quintile columns, where the
geography publishes them. Default \code{TRUE}.}

\item{population}{Logical. Attach ON-Marg's own 2021 population count.
Default \code{FALSE}, since boundary layers usually carry their own.}

\item{dimensions}{Character vector naming which of the four dimensions to
attach: any of \code{"households_dwellings"}, \code{"material_resources"},
\code{"age_labourforce"}, \code{"racialized_nc_pop"}. Defaults to all four.}

\item{refresh}{Logical, passed to \code{\link[=retrieve_onmarg]{retrieve_onmarg()}}.}
}
\value{
\code{x} with the requested \verb{onmarg_*} columns added. Features with no
ON-Marg row get \code{NA}; how many that was is reported in a message, because
a key mismatch otherwise looks exactly like a successful join.
}
\description{
Adds the 2021 Ontario Marginalization Index measures to a layer whose
features are one of the geographies ON-Marg publishes, matching on the
layer's own key column. The layer is returned unchanged apart from the added
\verb{onmarg_*} columns, so an \code{sf} object stays an \code{sf} object with its geometry
intact.
}
\details{
The ON-Marg name columns (\code{CSDNAME}, \code{HU_NAME}, and similar) are deliberately
not attached: they restate a name the boundary layer already carries, under a
name that would collide with it.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
phu <- retrieve_phu_pre2025()
add_onmarg(phu, "phu")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=retrieve_onmarg]{retrieve_onmarg()}}, \code{\link[=onmarg_geographies]{onmarg_geographies()}}.
}
