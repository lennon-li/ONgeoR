% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve}
\alias{resolve}
\title{Resolve records from a layer by an identifier or name}
\usage{
resolve(layer, query, by = c("ident", "name"), column = NULL, match = NULL)
}
\arguments{
\item{layer}{An \code{sf} object or \code{data.frame} with attribute columns.}

\item{query}{A character vector of one or more values to look up.}

\item{by}{Character. \code{"ident"} (default) uses the layer's id column with
exact matching; \code{"name"} uses the name column with substring matching.
The columns are auto-detected. Ignored when \code{column} is supplied.}

\item{column}{Character or \code{NULL}. Overrides the column to match against.}

\item{match}{Character or \code{NULL}. \code{"exact"} or \code{"substring"}. If \code{NULL}
(default), derived from \code{by} (\code{ident} -> exact, \code{name} -> substring).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with a \code{query} column, the layer's non-geometry
columns for matches, and \code{source_url} / \code{retrieved_at} provenance. A query
with no match yields one row with \code{NA} data columns; a single combined
warning lists all unmatched query values.
}
\description{
Attribute lookup (not a spatial operation): given one or more query values,
return the matching record(s) from a layer. By default matches an id column
exactly or a name column by substring; either can be overridden.
}
\examples{
if (interactive()) {
  airports <- retrieve_airport()
  resolve(airports, "CYYZ")
  resolve(airports, "toronto", by = "name")
}

}
