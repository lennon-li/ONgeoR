% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersection.R
\name{summarise_by_target}
\alias{summarise_by_target}
\title{Summarise an intersection or nearest table by target}
\usage{
summarise_by_target(pairs)
}
\arguments{
\item{pairs}{A tibble produced by \code{\link[=build_intersection]{build_intersection()}} or
\code{\link[=build_nearest_pairs]{build_nearest_pairs()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with exactly one row per distinct target.
Columns: \code{target_id}, \code{target_name}, \code{target_source}, \code{n_source},
\code{source_ids}, \code{source_names}, \code{shares_of_target}, \code{shares_of_source},
\code{dominant_source_id}, \code{dominant_source_name},
\code{dominant_share_of_target}, \code{covered_share}, \code{match_distance_km},
all \verb{tgt_*} attributes, and provenance columns.
}
\description{
Collapses a pairs table (from \code{\link[=build_intersection]{build_intersection()}} or
\code{\link[=build_nearest_pairs]{build_nearest_pairs()}}) to exactly one row per distinct target, with
multi-valued fields as \code{"; "}-delimited strings.
}
\examples{
if (interactive()) {
  pairs <- build_intersection(retrieve_municipal("upper"), retrieve_phu())
  summary_tbl <- summarise_by_target(pairs)
}

}
