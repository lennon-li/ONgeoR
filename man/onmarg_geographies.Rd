% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onmarg.R
\name{onmarg_geographies}
\alias{onmarg_geographies}
\title{ON-Marg geographies and the boundary layers they attach to}
\usage{
onmarg_geographies()
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{geography} (the token accepted by
\code{\link[=retrieve_onmarg]{retrieve_onmarg()}}), \code{label}, \code{sheet} (the workbook sheet it is read
from), \code{uid} (the key column as published), \code{target_key} (the column that
key appears under in the boundary layer), \code{source_id} (the ONgeoR source,
or \code{NA}), and \code{quintiles} (whether quintile columns are published for that
geography as well as factor scores).
}
\description{
Lists the geographies published in the 2021 Ontario Marginalization Index
(ON-Marg), the key column each one is published under, and the ONgeoR
boundary source whose features that key identifies. This is a static
correspondence table: it performs no download.
}
\details{
Two geographies (\code{"lhin"}, \code{"lhin_sr"}) have no \code{source_id}, because ONgeoR
does not currently provide Local Health Integration Network boundaries.
Their marginalization values are still retrievable with
\code{\link[=retrieve_onmarg]{retrieve_onmarg()}}; they simply cannot be attached to a layer.

ON-Marg 2021 reports Public Health Units on the pre-2025, 34-unit geography
(\code{phu_boundaries_pre2025}). There is no correspondence for the post-2025,
29-unit geography returned by \code{\link[=retrieve_phu]{retrieve_phu()}}.
}
\examples{
onmarg_geographies()

}
\seealso{
\code{\link[=retrieve_onmarg]{retrieve_onmarg()}} to fetch the values, \code{\link[=add_onmarg]{add_onmarg()}} to attach
them to a layer.
}
