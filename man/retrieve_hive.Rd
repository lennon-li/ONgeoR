% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_hive}
\alias{retrieve_hive}
\title{Retrieve HIVE Grid boundaries}
\usage{
retrieve_hive(refresh = FALSE)
}
\arguments{
\item{refresh}{Logical. Accepted for signature uniformity with the other
\verb{retrieve_*()} functions but unused: HIVE is a static built-in dataset
with no live source to re-fetch from. Defaults to \code{FALSE}.}
}
\value{
An \code{sf} object of HIVE Grid polygons (\code{GRID_ID}, \code{Level},
\code{HIVE_ID} columns) in EPSG:4326, with \code{source_url}, \code{source_name}, and
\code{retrieved_at} attributes attached for provenance.
}
\description{
Returns the built-in HIVE Grid dataset, a custom hierarchical polygon
grid (Levels 1-3, 1629 features) maintained by the package author. Unlike
the other \verb{retrieve_*()} functions, this does not call a live web
service: the data ships with the package as a static, pre-simplified
\code{sf} object (see \code{data-raw/hive.R} for the reproducible prep pipeline
that generated it from the author's source shapefile).
}
\examples{
if (interactive()) {
  hive <- retrieve_hive()
}

}
