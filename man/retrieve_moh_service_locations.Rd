% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_moh_service_locations}
\alias{retrieve_moh_service_locations}
\title{Retrieve MOH service locations}
\usage{
retrieve_moh_service_locations(
  service_type = NULL,
  refresh = FALSE,
  max_age = NULL
)
}
\arguments{
\item{service_type}{Character or \code{NULL}. If supplied, filters results to
rows where \code{SERVICE_TYPE} equals this value. If \code{NULL} (the default),
no filter is applied.}

\item{refresh}{Logical. If \code{TRUE}, bypasses any cached copy and re-fetches
from the live API. Defaults to \code{FALSE}.}

\item{max_age}{Numeric or \code{NULL}. Maximum acceptable cache age in days;
older entries are re-fetched. Defaults to \code{NULL}.}
}
\value{
An \code{sf} object of MOH service location points, with \code{source_url},
\code{source_name}, and \code{retrieved_at} attributes attached.
}
\description{
Retrieves Ministry of Health service location points (hospitals, clinics,
and other health service facilities) from the LIO Open Data REST service
(\code{LIO_Open09/26}), optionally filtered by service type.
}
\examples{
if (interactive()) {
  hospitals <- retrieve_moh_service_locations(service_type = "Hospital")
}

}
