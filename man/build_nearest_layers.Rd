% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{build_nearest_layers}
\alias{build_nearest_layers}
\title{Build layers for a nearest-neighbour map}
\usage{
build_nearest_layers(source, target, k = 1, max_dist_km = NULL)
}
\arguments{
\item{source}{An \code{sf} object of source point geometries.}

\item{target}{An \code{sf} object of candidate point or polygon geometries.}

\item{k}{Integer. Number of nearest targets per source. Defaults to \code{1};
\code{Inf} may be used with \code{max_dist_km} for a radius search.}

\item{max_dist_km}{Numeric or \code{NULL}. If set, omit targets farther than this
distance in kilometres.}
}
\value{
A named list with four elements: \code{source}, the original source
layer; \code{matched_target}, the target features present in the matches;
\code{connectors}, an \code{sf} layer of connector lines or \code{NULL} when there are no
matches; and \code{table}, the tibble returned by \code{\link[=nearest]{nearest()}}.
}
\description{
Finds the nearest target features for each source feature, retains only
matched targets, and constructs one connector line per match. This supports
custom map renderers that need the same layers as \code{\link[=map_nearest]{map_nearest()}}.
}
\examples{
stations <- retrieve_monitoring_stations_simple()
build_nearest_layers(stations[1:3, ], stations[4:10, ])

}
\seealso{
Other app support interfaces:
\code{\link[=extract_polygon_collection]{extract_polygon_collection()}},
\code{\link[=guess_name_col]{guess_name_col()}},
\code{\link[=layer_id_col]{layer_id_col()}},
\code{\link[=render_postal_reproducer_script]{render_postal_reproducer_script()}},
\code{\link[=render_reproducer_script]{render_reproducer_script()}}
}
\concept{app support interfaces}
