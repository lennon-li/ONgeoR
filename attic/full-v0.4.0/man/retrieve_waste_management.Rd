% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_waste_management}
\alias{retrieve_waste_management}
\title{Retrieve waste management site boundaries}
\usage{
retrieve_waste_management(simplify = FALSE, refresh = FALSE, max_age = NULL)
}
\arguments{
\item{simplify}{Logical. If \code{TRUE}, requests generalized geometry from the
service (\code{maxAllowableOffset = 1e-04}, i.e. 0.0001 degrees, since the
service returns EPSG:4326 -- roughly 11 m on the ground). Defaults to
\code{FALSE}: confirmed live that the simplified request returns corrupted
geometry for this layer (\code{GEOMETRYCOLLECTION} instead of polygons, for
all 813 features) rather than valid generalized boundaries -- the same
class of distortion documented for \code{\link[=retrieve_phu]{retrieve_phu()}}, caught here by
live-testing rather than assumed from feature count. Do not flip this
default without re-testing live.}

\item{refresh}{Logical. If \code{TRUE}, bypasses any cached copy and re-fetches
from the live API. Defaults to \code{FALSE}.}

\item{max_age}{Numeric or \code{NULL}. Maximum acceptable cache age in days;
older entries are re-fetched. Defaults to \code{NULL}.}
}
\value{
An \code{sf} object of waste management site boundary polygons, with
\code{source_url}, \code{source_name}, and \code{retrieved_at} attributes attached.
}
\description{
Retrieves waste management site boundaries from the LIO Open Data REST
service (\code{LIO_Open08/9}).
}
\examples{
\dontrun{
# Retrieves from the Ontario LIO REST service and caches the result.
waste_sites <- retrieve_waste_management()
}

}
