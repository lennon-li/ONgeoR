% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_phu}
\alias{retrieve_phu}
\title{Retrieve Public Health Unit boundaries}
\usage{
retrieve_phu(simplify = TRUE, refresh = FALSE, max_age = NULL)
}
\arguments{
\item{simplify}{Logical. If \code{TRUE} (the default), requests generalized
geometry from the service (\code{maxAllowableOffset = 1e-04}, i.e. 0.0001
degrees, since the service returns EPSG:4326 -- roughly 11 m on the
ground) to reduce payload size. Set to \code{FALSE} if you need full-precision
geometry, but be aware that the LIO service may fail to serve the
full-resolution layer intermittently; if that occurs, retry with
\code{simplify = TRUE} or set \code{refresh = TRUE}.}

\item{refresh}{Logical. If \code{TRUE}, bypasses any cached copy and re-fetches
from the live API. Defaults to \code{FALSE}.}

\item{max_age}{Numeric or \code{NULL}. Maximum acceptable cache age in days;
older entries are re-fetched. Defaults to \code{NULL}.}
}
\value{
An \code{sf} object of PHU boundary polygons, with \code{source_url},
\code{source_name}, and \code{retrieved_at} attributes attached for provenance.
}
\description{
Retrieves Ontario Public Health Unit (PHU) boundaries from the LIO
Open Data REST service (\code{LIO_Open09/44}).
}
\examples{
\dontrun{
# Retrieves from the Ontario LIO REST service and caches the result.
phu <- retrieve_phu()
}

}
