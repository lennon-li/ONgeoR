% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{facilities_within}
\alias{facilities_within}
\title{Find facilities within a radius of points}
\usage{
facilities_within(points, facilities = NULL, radius_km, service_type = NULL)
}
\arguments{
\item{points}{An \code{sf} object of points, or a \code{data.frame} with \code{lon} and
\code{lat} columns (assumed CRS 4326 / WGS 84).}

\item{facilities}{An \code{sf} point object of facilities, as returned by
\code{\link[=retrieve_moh_service_locations]{retrieve_moh_service_locations()}}. If \code{NULL} (the default), facilities
are retrieved automatically.}

\item{radius_km}{Numeric. Search radius in kilometers. Facilities with
\code{distance_km <= radius_km} are included.}

\item{service_type}{Character or \code{NULL}. Passed to
\code{\link[=retrieve_moh_service_locations]{retrieve_moh_service_locations()}} when \code{facilities} is \code{NULL}; ignored
when \code{facilities} is supplied directly.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the original point data, matched facility
attributes, \code{distance_km}, and \code{source_url} / \code{retrieved_at} provenance
columns. Points with no facilities within the radius contribute zero
rows. Distance calculation uses a full point-by-facility distance matrix,
which may be memory-intensive for large inputs.
}
\description{
Finds all Ministry of Health service location facilities within a radius
of each input point.
}
\examples{
if (interactive()) {
  points <- data.frame(lon = -79.3832, lat = 43.6532)
  result <- facilities_within(points, radius_km = 10)
}

}
