% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crosswalk.R
\name{build_crosswalk}
\alias{build_crosswalk}
\title{Build an auditable crosswalk table between two geographic layers}
\usage{
build_crosswalk(
  from,
  to,
  method = c("within", "intersects", "point_on_surface", "largest_overlap", "weighted")
)
}
\arguments{
\item{from}{An \code{sf} object: the source layer (e.g. municipal boundaries).}

\item{to}{An \code{sf} object: the target layer (e.g. PHU boundaries).}

\item{method}{Character. Assignment rule for the crosswalk:
\itemize{
\item \code{"within"} (default) / \code{"intersects"}: spatial-join predicate. If
\code{method = "within"} and \code{from} is polygonal while \code{to} is point-type,
the join direction is geometrically degenerate (a polygon is never
"within" a point). In that case \code{build_crosswalk()} auto-corrects by
joining \code{to} within \code{from} instead, emits an informative message, and
builds the same output schema from the corrected join. If \code{from} and
\code{to} are both point layers, containment has no meaningful direction to
auto-correct (a point is only within/intersects another point when
exactly coincident), so \code{build_crosswalk()} instead emits a warning
and proceeds with the join as-is; use \code{\link[=nearest]{nearest()}} or \code{\link[=build_link]{build_link()}}
for point-to-point matching.
\item \code{"point_on_surface"}: representative-point assignment for point-like
\code{from} polygons. Each \code{from} polygon is reduced to a single
guaranteed-interior point (\code{sf::st_point_on_surface()}, not the
centroid, which can fall outside a concave polygon) and joined
\code{"within"} the \code{to} boundaries. \code{to} must be a polygon layer.
\item \code{"largest_overlap"}: same-scale polygon-to-polygon assignment. Each
\code{from} polygon is assigned to the single \code{to} polygon it shares the
largest intersection area with, and the \code{coverage} column reports that
winner's share of the \code{from} polygon's area. Both layers must be
polygonal.
\item \code{"weighted"}: polygon-to-polygon apportionment. Every intersecting
\code{to} polygon is retained, with \code{coverage} reporting its share of the
\code{from} polygon's area. Coverage values for a \code{from} polygon sum to at
most 1, and equal 1 only when the \code{to} layer fully covers it. The
\code{largest_overlap} result is the argmax row of the weighted result.
}

\code{build_crosswalk()} returns an assignment table only: it never emits or
contains geometry. \code{"largest_overlap"} uses intersection internally as
area arithmetic only.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{from_id}, \code{from_name},
\code{from_source}, \code{to_id}, \code{to_name}, \code{to_source}, \code{match_method},
\code{match_distance_km} (always \code{NA}, reserved for nearest-neighbour
matching in a future version), \code{coverage} (the winner's area share for
\code{"largest_overlap"}, each intersecting pair's area share for \code{"weighted"},
otherwise \code{NA}), \code{from_id_col}, \code{to_id_col}, \code{source_url_from},
\code{source_url_to}, and \code{retrieved_at}.
}
\description{
Joins two \code{sf} layers (e.g. municipalities to Public Health Units) and
returns a tidy crosswalk table with full source provenance.
}
\section{Geometry combination matrix}{

What an operation does is determined by the geometry types of the two
layers, not by a match-rule argument:
\describe{
\item{point to point}{Nearest. Each target point is matched to its single nearest source point. Output: nearest table.}
\item{point to polygon}{Containment. Each point is matched to the boundary it falls inside. Output: crosswalk.}
\item{point to raster}{Sampling. Each point takes the value of the cell containing it. Output: linked values table.}
\item{polygon to point}{Containment. Direction is auto-corrected internally. Output: crosswalk.}
\item{polygon to polygon}{Intersection. Every overlapping pair, with the share of each target covered and the share of each source falling inside. Output: intersection table.}
\item{polygon to raster}{Sampling. Each polygon samples the raster values it overlaps. Output: linked values table.}
\item{raster to point}{Sampling. Raster reduced to cell centroids. Output: linked values table.}
\item{raster to polygon}{Cell sampling into boundaries. Each cell centroid is matched to the boundary it falls inside. Output: linked values table.}
\item{raster to raster}{Not supported. Not supported; align/resample with terra first. Output: none.}
}
}

\examples{
if (interactive()) {
  upper_tier <- retrieve_municipal("upper")
  phu <- retrieve_phu()
  crosswalk <- build_crosswalk(upper_tier, phu, method = "within")
}

}
\seealso{
\code{\link[=build_link]{build_link()}} picks the operation from the geometry pair with no
\code{method} argument. The "What linking does, by layer types" section of
\code{vignette("building-crosswalks", package = "ONgeoR")} tabulates the same
matrix, including where the \code{coverage} column comes from.
}
