% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{layer_id_col}
\alias{layer_id_col}
\title{Resolve the id column of a retrieved layer}
\usage{
layer_id_col(layer)
}
\arguments{
\item{layer}{An \code{sf} object or \code{data.frame}.}
}
\value{
Character scalar: the id column name.
}
\description{
Returns the column that identifies each feature of \code{layer}. When the layer
carries retrieval provenance, the answer comes from the source registry's
declared key fields, so it matches the \code{from_id_col} / \code{to_id_col} values
recorded in \code{\link[=build_crosswalk]{build_crosswalk()}} and \code{\link[=build_intersection]{build_intersection()}} output. Layers
without provenance fall back to a name-pattern guess.
}
\details{
This is the supported way for a downstream caller to join a crosswalk or
linked table back onto the layer geometry it came from.
}
\examples{
layer_id_col(data.frame(PHU_ID = 2253, PHU_NAME_ENG = "Example"))

}
\seealso{
Other app support interfaces:
\code{\link[=build_nearest_layers]{build_nearest_layers()}},
\code{\link[=extract_polygon_collection]{extract_polygon_collection()}},
\code{\link[=guess_name_col]{guess_name_col()}},
\code{\link[=render_postal_reproducer_script]{render_postal_reproducer_script()}},
\code{\link[=render_reproducer_script]{render_reproducer_script()}}
}
\concept{app support interfaces}
