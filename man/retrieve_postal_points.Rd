% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postal.R
\name{retrieve_postal_points}
\alias{retrieve_postal_points}
\title{Retrieve Ontario postal code points as a layer}
\usage{
retrieve_postal_points(bbox = NULL, refresh = FALSE)
}
\arguments{
\item{bbox}{Optional \code{sf} bbox or numeric \verb{xmin, ymin, xmax, ymax} vector in
EPSG:4326. When supplied, only points inside the envelope are returned.
The full province is 299,782 points, which is more than most maps or
spatial joins can carry, so window the layer whenever the use case allows.}

\item{refresh}{Logical. If \code{TRUE}, discards the cached release and
re-downloads it. Defaults to \code{FALSE}.}
}
\value{
An \code{sf} POINT layer in EPSG:4326 with \code{postal_code}, \code{point_source},
and \code{point_method} columns, and \code{source_name}, \code{source_url}, and
\code{retrieved_at} attributes attached for provenance. Postal codes without
coordinates (the 14 codes whose \code{point_source} is \code{"none"}) are dropped
silently, so the layer has one row per postal code that can be placed.
}
\description{
Returns every Ontario postal-code centroid in the OPCC M1 release as an \code{sf}
POINT layer, so postal codes can be used as a crosswalk or map layer without
supplying a list of codes. The release is downloaded, checksum-verified, and
cached by the same machinery \code{\link[=resolve_postal_points]{resolve_postal_points()}} uses.
}
\examples{
\dontrun{
# Whole province: 299,782 points.
postal_points <- retrieve_postal_points()

# A downtown Toronto window, which is what an interactive map should ask
# for.
toronto <- retrieve_postal_points(
  bbox = c(xmin = -79.42, ymin = 43.63, xmax = -79.36, ymax = 43.68)
)
nrow(toronto)
}

}
\seealso{
\code{\link[=resolve_postal_points]{resolve_postal_points()}} to look up a known list of postal codes.
}
