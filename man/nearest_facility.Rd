% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{nearest_facility}
\alias{nearest_facility}
\title{Find the nearest facilities to points}
\usage{
nearest_facility(points, facilities = NULL, k = 1, service_type = NULL)
}
\arguments{
\item{points}{An \code{sf} object of points, or a \code{data.frame} with \code{lon} and
\code{lat} columns (assumed CRS 4326 / WGS 84).}

\item{facilities}{An \code{sf} point object of facilities, as returned by
\code{\link[=retrieve_moh_service_locations]{retrieve_moh_service_locations()}}. If \code{NULL} (the default), facilities
are retrieved automatically.}

\item{k}{Integer. Number of nearest facilities to return per point.
Defaults to \code{1}. If \code{k} exceeds the number of facilities, all facilities
are returned for each point.}

\item{service_type}{Character or \code{NULL}. Passed to
\code{\link[=retrieve_moh_service_locations]{retrieve_moh_service_locations()}} when \code{facilities} is \code{NULL}; ignored
when \code{facilities} is supplied directly.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the original point data, a \code{rank} column,
matched facility attributes, \code{distance_km}, and \code{source_url} /
\code{retrieved_at} provenance columns. Distance calculation uses a full
point-by-facility distance matrix, which may be memory-intensive for
large inputs.
}
\description{
Finds the \code{k} nearest Ministry of Health service location facilities for
each input point.
}
\examples{
if (interactive()) {
  points <- data.frame(lon = -79.3832, lat = 43.6532)
  result <- nearest_facility(points, k = 3, service_type = "Hospital")
}

}
