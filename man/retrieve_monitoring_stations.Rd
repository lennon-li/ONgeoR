% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_monitoring_stations}
\alias{retrieve_monitoring_stations}
\title{Retrieve monitoring station points}
\usage{
retrieve_monitoring_stations(simplify = TRUE, refresh = FALSE, max_age = NULL)
}
\arguments{
\item{simplify}{Logical. If \code{TRUE} (the default), requests generalized
geometry from the service. Monitoring stations are point features, so
simplification has no visible effect but is kept for consistency with
the other LIO retrievers.}

\item{refresh}{Logical. If \code{TRUE}, bypasses any cached copy and re-fetches
from the live API. Defaults to \code{FALSE}.}

\item{max_age}{Numeric or \code{NULL}. Maximum acceptable cache age in days;
older entries are re-fetched. Defaults to \code{NULL}.}
}
\value{
An \code{sf} object of monitoring station points, with \code{source_url},
\code{source_name}, and \code{retrieved_at} attributes attached for provenance.
}
\description{
Retrieves Ontario water and weather monitoring station point locations from
the LIO Open Data REST service (\code{LIO_Open08/30}).
}
\examples{
\dontrun{
stations <- retrieve_monitoring_stations()
}

}
