% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retrieve.R
\name{retrieve_municipal}
\alias{retrieve_municipal}
\title{Retrieve municipal boundaries}
\usage{
retrieve_municipal(
  tier = c("upper", "lower"),
  simplify = TRUE,
  refresh = FALSE,
  max_age = NULL
)
}
\arguments{
\item{tier}{Character. Either \code{"upper"} (upper-tier and district
municipalities) or \code{"lower"} (lower-tier and single-tier
municipalities). Defaults to \code{"upper"}.}

\item{simplify}{Logical. If \code{TRUE} (the default), requests generalized
geometry from the service.}

\item{refresh}{Logical. If \code{TRUE}, bypasses any cached copy and re-fetches
from the live API. Defaults to \code{FALSE}.}

\item{max_age}{Numeric or \code{NULL}. Maximum acceptable cache age in days;
older entries are re-fetched. Defaults to \code{NULL}.}
}
\value{
An \code{sf} object of municipal boundary polygons, with \code{source_url},
\code{source_name}, and \code{retrieved_at} attributes attached.
}
\description{
Retrieves Ontario municipal boundaries from the LIO Open Data REST
service, either the upper/district tier (\code{LIO_Open03/13}) or the
lower/single tier (\code{LIO_Open03/14}).
}
\examples{
if (interactive()) {
  upper_tier <- retrieve_municipal("upper")
}

}
