% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link.R
\name{polygon_to_polygon}
\alias{polygon_to_polygon}
\title{Join a polygon layer to another polygon layer}
\usage{
polygon_to_polygon(from, to)
}
\arguments{
\item{from}{An \code{sf} object of polygons to be joined (e.g. municipalities).}

\item{to}{An \code{sf} object of polygons to join to (e.g. PHU boundaries).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with the attributes of \code{from} joined to the
matching attributes of \code{to}, plus \code{source_url_to} / \code{retrieved_at}
provenance columns describing \code{to}.
}
\description{
Performs a spatial join between two polygon layers, e.g. municipalities
to Public Health Units. Uses \code{\link[sf:st_join]{sf::st_join()}} with an intersects
predicate.
}
\examples{
if (interactive()) {
  upper_tier <- retrieve_municipal("upper")
  phu <- retrieve_phu()
  result <- polygon_to_polygon(upper_tier, phu)
}

}
