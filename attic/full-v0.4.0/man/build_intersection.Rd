% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersection.R
\name{build_intersection}
\alias{build_intersection}
\title{Build a polygon-to-polygon intersection table}
\usage{
build_intersection(source, target, min_overlap = 0)
}
\arguments{
\item{source}{An \code{sf} polygon layer (the matched unit).}

\item{target}{An \code{sf} polygon layer (the index unit).}

\item{min_overlap}{Numeric. Minimum intersection area in square metres for a
pair to be retained. Defaults to \code{0}, meaning strictly greater than zero
(boundary-touching polygons that share only an edge are excluded).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per overlapping pair (plus
all-NA-match rows for unmatched targets). Fixed columns: \code{interaction_id},
\code{target_id}, \code{target_name}, \code{target_source}, \code{source_id}, \code{source_name},
\code{source_source}, \code{relation}, \code{overlap_area_m2}, \code{share_of_target},
\code{share_of_source}, \code{match_distance_km}, then every source attribute
prefixed \code{src_}, every target attribute prefixed \code{tgt_}, then
\code{source_url_source}, \code{source_url_target}, \code{retrieved_at}, \code{simplify_used}.
No geometry column is ever emitted.
}
\description{
Computes every overlapping pair between a source polygon layer and a target
polygon layer using a single vectorized \code{sf::st_intersection()} call, and
returns one row per pair with area shares. Targets with no overlap at all
receive one explicit all-NA-match row so every target feature is represented.
}
\examples{
hive <- retrieve_hive()
cells <- hive[hive$Level == "Level 3", ][1:50, ]
build_intersection(cells, retrieve_phu_simple())

}
