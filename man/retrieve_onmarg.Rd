% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/onmarg.R
\name{retrieve_onmarg}
\alias{retrieve_onmarg}
\title{Retrieve Ontario Marginalization Index (ON-Marg) values}
\usage{
retrieve_onmarg(geography, refresh = FALSE)
}
\arguments{
\item{geography}{Character scalar naming the geography, e.g. \code{"da"} or
\code{"phu"}. See \code{\link[=onmarg_geographies]{onmarg_geographies()}} for the full set.}

\item{refresh}{Logical. \code{TRUE} re-downloads the workbook instead of reusing
the copy held for this session. Default \code{FALSE}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per feature of that geography: the
key column under its published name (character), the \verb{onmarg_*} measure
columns (numeric), and the geography's own name column where the workbook
publishes one. Carries \code{source_name}, \code{source_url}, \code{retrieved_at}, and
\code{citation} attributes for provenance.
}
\description{
Fetches one geography's worth of the 2021 Ontario Marginalization Index from
Public Health Ontario. ON-Marg summarises four dimensions of marginalization
derived from the 2021 Census: households and dwellings (formerly residential
instability), material resources (formerly material deprivation), age and
labour force (formerly dependency), and racialized and newcomer populations
(formerly ethnic concentration).
}
\details{
The workbook is downloaded at runtime, verified against a pinned SHA-256,
and held in memory for the life of the R process. Unlike ONgeoR's spatial
retrievals it is never written to the ONgeoR cache directory: its licence
permits non-commercial use with attribution and forbids modifying the
content, so ONgeoR neither redistributes nor stores it. Values are returned
exactly as published; only column names are normalised, because the
published names carry a per-sheet geography suffix:
\itemize{
\item \code{Pop2021} / \code{pop2021} becomes \code{onmarg_pop2021}
\item \verb{households_dwellings_*} becomes \code{onmarg_households_dwellings}
\item \verb{material_resources_*} becomes \code{onmarg_material_resources}
\item \verb{age_labourforce_*} becomes \code{onmarg_age_labourforce}
\item \verb{racialized_NC_pop_*} becomes \code{onmarg_racialized_nc_pop}
\item the matching \verb{*_q_*} columns take the same names with a \verb{_q} suffix
}

Higher factor scores mean more marginalization on that dimension. Quintiles
run 1 (least marginalized) to 5 (most), and are published for some
geographies only - see \code{\link[=onmarg_geographies]{onmarg_geographies()}}.

Cite ON-Marg as: Matheson FI (Unity Health Toronto), Moloney G (Unity
Health Toronto), van Ingen T (Public Health Ontario). 2021 Ontario
marginalization index. Toronto, ON: St. Michael's Hospital (Unity Health
Toronto); 2023.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
retrieve_onmarg("phu")
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=add_onmarg]{add_onmarg()}} to attach these values to a boundary layer,
\code{\link[=onmarg_geographies]{onmarg_geographies()}} for the available geographies.
}
