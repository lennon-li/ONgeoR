% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve_airport}
\alias{resolve_airport}
\title{Resolve airport records from an identifier or name}
\usage{
resolve_airport(query, by = c("ident", "name"), airports = NULL)
}
\arguments{
\item{query}{A character vector of one or more values to look up.}

\item{by}{Character. Either \code{"ident"} to match \code{AIRPORT_IDENT} exactly,
ignoring case, or \code{"name"} to match \code{NAME} by case-insensitive substring.
Defaults to \code{"ident"}.}

\item{airports}{An \code{sf} object of airport boundaries, as returned by
\code{\link[=retrieve_airport]{retrieve_airport()}}. If \code{NULL} (the default), airport boundaries are
retrieved automatically.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with a \code{query} column, all matching airport
attribute columns, and \code{source_url} / \code{retrieved_at} provenance columns.
Query values with no match return one row with \code{NA} airport attributes.
}
\description{
Looks up one or more airport records from the airport layer returned by
\code{\link[=retrieve_airport]{retrieve_airport()}}. Identifier lookups match \code{AIRPORT_IDENT} exactly,
ignoring case. Name lookups match \code{NAME} by case-insensitive substring.
}
\examples{
if (interactive()) {
  airport <- resolve_airport("CYYZ")
  toronto_airports <- resolve_airport("toronto", by = "name")
}

}
